[package]
name = "hw2"
version = "0.1.0"
edition = "2024"

[dependencies]
itertools = "0.14.0"

[dev-dependencies]
rand = "0.9.1"

[lints.rust]
warnings = "forbid"
unsafe-code = "forbid"

[lints.clippy]
all = "deny"
